# Environment
.env
.envrc

# Docker volume (SQLite DB + uploads, when using docker-compose)
instance/

# OS / editor
.DS_Store
.idea/
.vscode/

# Playwright MCP browser-tool artifacts (snapshots, console logs)
.playwright-mcp/

.omc/

# The Next.js app manages its own ignores (node_modules, .next, *.db, etc.)
# — see web/.gitignore.
