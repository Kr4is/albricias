# A GitHub personal access token with no special scopes, used server-side
# for every visitor's read-only activity lookup (visitors only supply a
# username, never a token). Without it, GitHub's unauthenticated rate limits
# (10 req/min on the search API) are exhausted almost immediately.
GITHUB_TOKEN=
