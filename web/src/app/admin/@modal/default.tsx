/**
 * Required for the `@modal` parallel slot: what renders there on every
 * `/admin/*` route that isn't the intercepted one (i.e. almost always).
 */
export default function Default() {
  return null;
}
